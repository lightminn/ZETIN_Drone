# BMM350 Yaw 융합 — HW 벤치 테스트 절차

`dual_imu_cascade_pwm` + BMM350 yaw 융합(branch `feat/bmm350-yaw-fusion`) 대상.
SIL은 융합 수학만 검증한다. 이 절차가 실센서·실간섭을 검증한다.

명령/텔레메트리 (빌드 확정): **`magcal 1`**=캘리브 시작(disarmed에서만), **`magcal 0`**
=중지+샘플 수·축별 span 출력, **`mag 1`/`mag 0`**=융합 ON/OFF(기본 OFF), **`magc x y z`**
=모터 전류 간섭 보정 계수(µT/µs) 3축 런타임 설정. 텔레메트리 **`MagHeading`**(31번,
틸트+간섭보정 heading) + **`Yaw`**(=angleZ, 융합 heading) + **`Mag_X/Y/Z`**(32~34번,
평상시 보정 후 자기장, `Mag_Cal_Active=1` 동안 보정 전 raw µT) +
**`Mag_Cal_Active`**(65번, 캡처 구간 식별).

> **결과 요약 (2026-07-27 완료):** 1~3 통과, 4에서 모터 전류 간섭 발견(호버 +13°,
> corr 0.80) → **raw XYZ 도메인 보정으로 제거**(heading slope +3.64→+0.02°/100µs).
> 계수 `mag_comp_x/y/z` 펌웨어 baking + 커밋(1497d74). 상세는 아래 4단계.

> **정정 (2026-08-04):** 아래 2026-07-27 결과는 당시 min/max hard-iron
> 절차로 얻은 날짜 기록이므로 수정하지 않는다. 현재 절차는 min/max 상수를
> 출력하지 않고, raw CSV를 Li & Griffiths 제약 타원체 + MAD 클리핑으로 피팅해
> hard/soft-iron을 함께 갱신한다.

## 0. 안전 / 준비
- **프롭 OFF** 로 시작(1~3단계는 무추력). 4단계만 프롭 ON.
- BMM350는 PCB 상 고정(움직이면 캘리브 무의미).
- 주변 **강자성체(공구·스피커·철제 책상다리) 치우기** — hard-iron 오염.
- 텔레메트리 모니터: `Yaw`(융합 heading) + `MagHeading`(틸트·간섭보정 heading) 관찰.
- ⚠️ BMM350 축 부호 기본 `+X/+Y/+Z` (미확인) — 2단계에서 회전과 heading이 **반대**로 가면 부호 반전 필요(아래 참조).

## 1. hard/soft-iron 캘리브레이션 (프롭 OFF, **disarmed**)
1. 지상국 CSV 로그가 기록 중인지 확인하고 `magcal 1`을 보낸다(armed면 거부).
   텔레메트리 `Mag_Cal_Active=1`과 변하는 raw `Mag_X/Y/Z`를 확인한다.
2. 드론을 **모든 방향으로 천천히 회전**한다. yaw 2~3바퀴뿐 아니라
   roll/pitch도 각각 충분히 기울여 구면 방향 커버리지를 만든다.
3. `magcal 0`을 보낸다. 시리얼의 샘플 수와 X/Y/Z span을 캡처 기록과
   대조한다. 펌웨어는 여기서 offset을 계산하거나 출력하지 않는다.
4. 저장소 루트에서 다음을 실행한다. 기본 동작은 CSV의
   `Mag_Cal_Active == 1` 행만 자동 선택한다.

   ```bash
   /home/light/anaconda3/bin/python scripts/magcal_fit.py logs/flight_log_YYYY-MM-DD_HHMMSS.csv
   ```

5. 커버리지 경고/거부, 피팅 반경·축비, 보정 전후 `|B|` 산포와 95퍼센타일
   radial residual을 확인한다. radial residual은 구면 피팅 오차이지 heading
   정확도가 아니다. 통과한 출력의 `MAG_HARD_IRON`과 `MAG_SOFT_IRON` 블록을
   펌웨어에 붙여 넣고, 함께 출력된 `W @ 기존계수` 값으로 `mag_comp_x/y/z`를
   갱신한다.
6. 재빌드·재플래시한 뒤 새 로그로 `|B|` 산포와 heading 안정성을 다시
   확인한다. 소프트아이언을 바꾸면 `mag_comp` 의미도 바뀌므로 4단계 모터
   간섭 검증도 반드시 다시 수행한다.

## 2. Heading 추종 (프롭 OFF)
- `mag 1` 로 융합 ON. 드론을 **천천히 회전**(폰 나침반으로 실제 방위 참고 가능).
- **불변식**: 기체를 한쪽으로 돌리면 `Yaw`(heading)도 그 방향으로 변하고, 멈추면 그 값에 **머문다**. 회전과 heading이 **반대로 가면 부호 반전** → 정지 후 firmware의 mag 축 부호/틸트보정 부호를 뒤집어 재검증(SIL "tilt compensation inverted"가 이걸 잡는 케이스).
- 틸트 섞어 회전해도(기울인 채 yaw) `MagHeading`이 흔들리지 않는지(틸트보정 확인).

## 3. 무드리프트 확인 (프롭 OFF) — 핵심
- 드론을 **한 방위로 고정하고 수 분 정지**. `Yaw` 로그를 받는다.
- **비교**: `mag 0`(자이로만)일 때는 수 분에 걸쳐 heading이 서서히 드리프트(°/분)하고, `mag 1`일 때는 **한 값에 고정**돼야 한다. 이 차이가 이 기능의 존재 이유.
- 통과 기준: `mag 1` 5분 드리프트 < 자이로온리 대비 현저히 작음(사실상 0 근처).

## 4. 모터 간섭 (프롭 ON, 고정·저속) — 최대 리스크
- **기체 단단히 고정, 프롭 ON, PSU 전류제한.** `mag 1`. **방위 고정**(회전 금지 —
  회전시키면 heading 변화가 throttle 효과를 덮는다).
- **반드시 올렸다가 내린다.** 예: `1100→1200→1300→1400→1300→1200→1100`,
  레벨당 5~10초(융합 τ≈4s 정착 대기).
  단조 상승만 하면 `corr(throttle, 시간) ≈ 0.99`가 되어 **전류 간섭과 시간
  드리프트를 분리할 수 없다.** 내림 구간이 그 둘을 가르는 유일한 장치다.
- **판정**은 `Mag_X/Y/Z`(µT)와 `MagHeading`을 throttle에 회귀해서 한다.
  - `MagHeading` 기울기 **±0.5°/100µs 이내, corr < 0.2** → 간섭 허용 ✅
  - 기울기가 크고 corr > 0.7 → 간섭 심함 → 완화 필요
  - 회귀 전에 `corr(throttle, 시간)`이 0에 가까운지 먼저 확인한다. 크면 그
    데이터로는 아무것도 판정할 수 없다.
- **가역성**: 같은 throttle의 올림/내림 값을 비교한다. 일치하면 전류 간섭
  (보정 가능), 한 방향으로만 어긋나면 시간 드리프트(계수로 고치면 안 된다).
- 계수를 고칠 때는 `magc x y z`로 **런타임에 먼저 시험**한다(재플래시 불필요).
  잔차가 실제로 줄어드는 것을 확인한 뒤에 굽는다.

### 실측 결과 + 완화 (2026-07-27)
간섭이 실재했다: 호버에서 **+13°**, `corr(throttle, MagHeading)=0.80`, 올림/내림 가역 →
시간 드리프트가 아닌 전류 간섭. **heading(°) 도메인 slope는 방위마다 달라 재현 안 됨**
(런별 +1.95 / +4.6 / +3.64°/100µs). 원인: 간섭은 body-frame ΔB(µT) 벡터인데 heading
투영이 방위 의존.
- **해법 = raw XYZ(µT) 도메인 보정**(방위 무관). `Mag_X/Y/Z`를 throttle에 회귀 →
  ΔB per µs 피팅(주로 X축: +0.0075µT/µs). 펌웨어가 `mag.{x,y,z} -= mag_comp_{x,y,z}·
  (base_throttle−1000)`을 틸트보정 전에 적용. 런타임 `magc x y z`, 기본값 baking.
- **검증(log 024330, comp ON):** MagHeading throttle slope **+3.64 → +0.02°/100µs**
  (corr 0.008), `Mag_X` slope ≈ 0. 잔차 ~2.8°는 throttle-무관 노이즈(융합 τ≈4s가 흡수).
- **확정 계수:** `mag_comp_x=+0.007497 mag_comp_y=−0.001218 mag_comp_z=−0.000640`
  (log 023805 특성화). **CAVEAT:** 계수 크기는 배터리 상태로 ~2× 변동 → 대부분 제거하나
  방전 시 잔차 증가. 90° 방위 무관 확인은 생략(구조상 방위 무관).

## 통과 기록 (2026-07-27 완료)
- [x] 1: 캘리브 후 정지 heading 안정 — offsets X=−1.831376 Y=9.318241 Z=−12.517762
- [x] 2: 회전에 heading 정방향 추종 + 틸트에도 안정 (BMM350 축 부호 그대로 OK)
- [x] 3: `mag 1` 무드리프트 (자이로온리 18.3° 대비 융합 2.4°)
- [x] 4: 간섭 발견(호버 +13°) → XYZ-domain 보정으로 제거(+0.02°/100µs) — 계수 baking

**4까지 완화 완료 → 비행 중 mag-yaw heading-hold 사용 가능** (배터리 의존 잔차 caveat 위 참조).
mag은 여전히 기본 OFF이며, 비행 heading-hold를 쓰려면 `mag 1`. 아직 실비행 검증은 미실시.

## 통과 기록 (2026-08-04 — 캘리브레이션 재작업)

위 2026-07-27 기록은 그대로 둔다. 1단계를 min/max에서 제약 타원체 피팅으로
바꾸고 재실시한 결과다. 설계 근거는
[`docs/superpowers/specs/2026-08-04-magnetometer-calibration-design.md`](superpowers/specs/2026-08-04-magnetometer-calibration-design.md).

- [x] 1: hard/soft-iron 재캘리브 + **보드 실측 검증 완료**
  - 캡처 A `flight_log_2026-08-04_023840.csv` (1255샘플, 커버리지비 2.08)에서
    상수 산출 → 펌웨어 baking + 플래시.
  - **재현성**: 독립 캡처 B `..._025241.csv`로 따로 피팅한 중심이 A와
    0.62 µT 차이(heading 1.11°). soft-iron 행렬은 0.02% 일치.
  - **out-of-sample**: A로 만든 상수를 B에 적용 → `|B|` 산포 1.60%
    (in-sample 1.75%와 동등 → 과적합 없음).
  - **보드 실측** `..._030620.csv` (46초, Yaw 1059°/Roll 276°/Pitch 300°,
    이상치 0): 펌웨어가 출력한 보정 자기장 `|B| = 50.83 ± 1.61 µT`,
    산포 **3.18%**. 같은 데이터를 옛 min/max 상수로 환산하면 7.80%,
    무보정이면 13.11%. `..._030320.csv`에서는 5.60% → **1.44%**.
  - **물리 정합성**: 50.83 µT vs 한국 총강도 실측 ~50.4 µT (0.9% 일치).
  - 옛 min/max 상수는 새 값에서 6.79 µT 벗어나 있었다 = 수평성분 32.2 µT
    대비 최대 **±12.2°** heading 오차.
- [x] 2·3: 2026-07-27 결과 유지(축 부호·틸트 안정·무드리프트는 이번 변경과 무관).
- [x] **4: 재실시 통과** (log `..._032922.csv`). soft-iron 행렬 도입으로
  `mag_comp_*`가 사는 좌표계가 바뀌어 `W @ 기존계수`로 환산해 구웠고
  (`0.007467 / −0.001150 / −0.000526`), 그 환산이 실측으로 검증됐다.
  - 램프 `1100→1200→1300→1400→1300→1200→1100`, 레벨당 ~5s, 방위 고정.
  - **`MagHeading` throttle 기울기 −0.067°/100µs (corr −0.030)** → 호버
    1360에서 −0.24°. 2026-07-27 검증통과값 +0.02°/100µs와 동급.
    `Mag_X` 잔차 +0.0008 µT/µs (corr 0.195).
  - 계수는 **조정하지 않았다.** 잔차 회귀가 제안한 값은 corr 0.2~0.25의
    약한 신호라 heading 기울기가 이미 0인 상태에서 적용하면 노이즈 학습이다.
  - **히스테리시스 = 시간 드리프트.** 같은 throttle에서 내림이 올림보다
    일관되게 낮다(−2.27 / −4.28 / −2.26°, 평균 2.94°). 한 방향 오프셋이므로
    throttle이 아니라 57초에 걸친 드리프트이고, 2026-07-27이 기록한
    "잔차 ~2.8°는 throttle-무관" 과 일치한다. 융합 τ≈4s가 흡수한다.

> **⚠️ 절차 함정 (2026-08-04에 실제로 걸림):** 단조 상승만 하는 램프는
> `corr(throttle, 시간) = 0.992`가 되어 **전류 간섭과 시간 드리프트를 수학적으로
> 분리할 수 없다.** 그렇게 측정한 첫 런은 +0.93°/100µs가 나와 불합격으로
> 오판했고, 그 잔차로 계수를 고쳤다면 드리프트를 과보정했을 것이다. 내림
> 구간을 넣자 `corr(throttle, 시간)`이 0.080으로 떨어지며 진짜 값
> −0.067°/100µs가 드러났다. **내림 램프는 선택이 아니라 판별 장치다.**

**잔차의 한계**: 같은 방에서 몇 분 간격의 캡처들이 `|B|` 46.50 / 48.46 /
50.10 / 50.83 µT로 ~9% 흩어진다. 실내 자기장의 공간 불균일(철골·배선)이며
캘리브레이션으로 줄일 수 없다. 크게 굴린 캡처일수록 방을 더 훑어 산포가
커지는 것도 같은 이유다. 실외에서는 더 낮게 나올 것으로 예상한다.
